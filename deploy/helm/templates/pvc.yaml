apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "mechanic.fullname" . }}-pvc
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 20Gi
  volumeMode: Filesystem
  storageClassName: static-nfs-storage-class
